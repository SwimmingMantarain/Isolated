# Isolated (W.I.P.)

## How to build & run
```bash
# To Build
git clone --depth 1 https://github.com/SwimmingMantarain/Isolated.git
cd Isolated
zig build

# To Run on Linux
./zig-out/bin/isolated

# To Run on Windows (haven't tested yet)
.\zig-out\bin\isolated.exe
```
